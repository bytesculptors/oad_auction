export default function RoomsLayout({ children }: { children: React.ReactNode }) {
    return (
        <section>
            {/* <div>header</div> */}
            {children}
            {/* <div>footer</div> */}
        </section>
    );
}
